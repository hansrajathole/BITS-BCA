<%- include("partial/header.ejs") %>
<main>
    <div class="container">

        <% if(success && success.length > 0 ){ %>
             <div id="toast">
                <%= success  %>
               </div>
        <% } %>
        <% if(error && error.length > 0 ){ %>
             <div id="error">
                <%= error  %>
               </div>
        <% } %>
    <div class="posts">
        <%- include("partial/post.ejs" ,{postData}) %>
    </div>

<%- include("partial/tools.ejs", {user}) %>

    </div>
</main>
<%- include("partial/footer.ejs") %>


